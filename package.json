{
  "name": "botu",
  "version": "0.2.1",
  "description": "BoomTube — an installable dotfiles + workspace engine (executable: botu). apply/verify/fix your machine from a declarative botufile.toml, and open portals to your code workspaces.",
  "type": "module",
  "private": true,
  "bin": {
    "botu": "./build/botu"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile build/botu",
    "test": "bun test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "check": "biome check . && tsc --noEmit && bun test",
    "site:build": "bun run site/build.ts"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "@stricli/core": "1.2.7",
    "smol-toml": "1.6.1",
    "valibot": "1.4.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "@types/bun": "1.3.14",
    "marked": "^18.0.6",
    "typescript": "6.0.3"
  }
}
