*.swp
.DS_Store
# Generated by `boom init` into the dotfiles repo — not a tracked source file.
examples/dotfiles/boominit.sh

# TypeScript / Bun
node_modules/
build/
*.tsbuildinfo

# Claude Code session worktrees. Each one carries its own checked-out copy of
# biome.json — with vcs.useIgnoreFile on, gitignoring this keeps Biome's project
# discovery from treating those copies as competing nested root configs ("Found a
# nested root configuration" errors breaking `bun run lint` from the main tree).
.claude/worktrees/

# Docs-site pages generated by `bun run site/build.ts` (built fresh in the Pages
# workflow). Sources are site/index.html + site/build.ts (the generator lifts the
# landing's inline <style>/<script> as the shared chrome); these are build artifacts.
site/spec.html
site/sitemap.xml
site/robots.txt
